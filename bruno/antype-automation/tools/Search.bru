meta {
  name: Search
  type: http
  seq: 1
}

post {
  url: {{host}}/search
  body: json
  auth: inherit
}

body:json {
  {
    "search_name": "journal template",
    "space_name": "journal",
    "query": " - "
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
