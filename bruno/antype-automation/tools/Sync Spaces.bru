meta {
  name: Sync Spaces
  type: http
  seq: 2
}

post {
  url: {{host}}/sync_spaces
  body: json
  auth: inherit
}

body:json {
  {
    "source_space_name": "tasks",
    "target_space_name": "journal"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
